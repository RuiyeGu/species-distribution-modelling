# EcoStat Modelling -- DATA5925: MLP & validation
#
# Exact environment in which the committed results were produced. Verified to
# reproduce the archived results bit-for-bit on all 42 evaluable AUC cells and
# all 48 F1/sensitivity/specificity cells; log loss and Brier agree to within
# one unit of float64 machine epsilon (2.2e-16).
#
# Python 3.12.4  (requirements.txt cannot pin the interpreter -- use this version)
#
#   python3.12 -m venv venv
#   ./venv/bin/python -m pip install -r requirements.txt
#
# This is the FULL pinned closure, not just the direct imports. joblib and
# threadpoolctl control scikit-learn's BLAS thread pool, and the thread count
# changes floating-point summation order -- so the bit-level reproducibility
# above holds only with the transitive packages pinned too.

# --- direct dependencies (imported by project scripts) ---
matplotlib==3.11.0
numpy==2.4.6
pandas==3.0.3
scikit-learn==1.8.0
scipy==1.17.1
xgboost==3.3.0

# --- transitive (pinned to preserve numerical reproducibility) ---
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
joblib==1.5.3
kiwisolver==1.5.0
packaging==26.2
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
threadpoolctl==3.6.0
